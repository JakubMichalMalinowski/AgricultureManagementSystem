@model IEnumerable<AgricultureManagementSystem.Models.Combine>

@{
    ViewData["Title"] = "Lista kombajnów";
}

<h1>Kombajny</h1>

<p>
    <a class="btn btn-success" asp-action="Create">Dodaj kombajn</a>
</p>
@if (Model.Count() > 0)
{
    <table class="table">
        <thead>
            <tr>
                <th class="align-middle">
                    @Html.DisplayNameFor(model => model.Brand)
                </th>
                <th class="align-middle">
                    @Html.DisplayNameFor(model => model.Model)
                </th>
                <th class="align-middle">
                    @Html.DisplayNameFor(model => model.ProductionYear)
                </th>
                <th class="align-middle">
                    @Html.DisplayNameFor(model => model.NumberOfStrawWalkers)
                </th>
                <th class="align-middle">
                    @Html.DisplayNameFor(model => model.Course)
                </th>
                <th class="align-middle">
                    @Html.DisplayNameFor(model => model.Power)
                </th>
                <th class="align-middle">
                    @Html.DisplayNameFor(model => model.FuelType)
                </th>
                <th class="align-middle">
                    @Html.DisplayNameFor(model => model.FuelCapacity)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Brand)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Model)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.ProductionYear)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.NumberOfStrawWalkers)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Course)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Power)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.FuelType)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.FuelCapacity)
                    </td>
                    <td>
                        <a class="btn btn-info" asp-action="Details" asp-route-id="@item.Id">Szczegóły</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <p>W tej chwili nie ma żadnych elementów do wyświetlenia, kliknij przycisk dodaj nowy, aby dodać pierwszy element.</p>
}
